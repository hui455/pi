# 安全策略

本文档旨在帮助你理解 Pi 背后的安全理念以及安全边界在哪里。

一般来说，Pi 是一个在运行它的用户的安全边界内本地运行的编码代理（coding agent）。监控其操作或将其限制在容器、虚拟机或其他沙箱解决方案内，是用户的责任。

Pi 将本地用户账户及该账户可写的文件视为与 Pi 进程本身处于同一信任边界内。如果攻击者可以修改用户主目录、workspace、shell 启动文件、环境或 Pi 配置下的文件，他们通常可以影响 Pi 或其他本地开发工具。依赖此类既有本地写入权限的报告不属于安全漏洞，除非它们能证明 Pi 如何授予该写入权限或跨越了操作系统特权边界。

Pi 依赖用户安装可信的扩展、加载可信的 skills，并且只在受信任的仓库中使用 pi。这是因为像 `AGENTS.md` 这样的文件或注释中的指令可以被轻易地用于对编码代理进行提示注入（prompt injection），而这无法防护。

## 报告漏洞

如果你认为自己在本仓库的 pi 或其他包中发现了安全漏洞，请通过以下任一方式私下报告：

- 发送邮件至 `security@earendil.com`，或
- 通过本仓库的 GitHub Security Advisories 开启私人报告

请包含：

- 问题的描述及其影响
- 复现步骤、概念验证或相关日志
- 受影响的包、版本、提交或配置
- 任何已知的缓解措施

请勿为敏感的安全报告开启公开 issue。我们会审查报告并酌情协调披露。

## 范围

已发布包、命令行工具、API 和仓库代码中的安全问题均在范围内，`pi.dev` 上由 earendil 运营的基础设施也在范围内。

## 范围之外

- 本地代码执行或沙箱行为（Pi 编码代理有意不提供沙箱）
- 用户安装的 pi 扩展或 skills 的行为
- 在不受信任的仓库中工作的风险
- 安装不受信任的扩展、skills、包或工具的风险
- 由不受信任的 MITM 代理引起的问题
- Pi 安装暴露于公网
- 提示注入攻击
- 暴露的、由第三方/用户控制的凭据
- 要求具备在目标机器上创建、修改、删除或替换文件、目录、符号链接、环境变量、shell 配置或其他用户可控的本地状态的能力的报告。这包括 `~/.pi`、`~/.pi/agent/models.json`、workspace 文件、`AGENTS.md`、skills、扩展、扩展配置、dotfiles，以及通过 NFS、漫游配置文件或 dotfile 管理器同步的文件，除非报告表明 Pi 本身授予了该访问权限。
- 由故意弱化的用户配置引起的问题。
- 需要针对 pi 编码代理提供受信任的本地输入/配置的资源/DOS 声明。
- 关于恶意模型输出的报告。
- 将用户已批准或用户主动发起的本地操作描述为漏洞的报告。

## 给报告者的说明

最有用的报告应展示当前可复现的、具有已证影响的安全边界绕过。只展示预期的本地代理行为、提示注入或恶意的受信任扩展/skill 的报告，在本模型下不属于安全漏洞。

例如，一份报告表明写入受信任的 Pi 配置文件中的恶意内容导致 Pi 执行命令、加载攻击者控制的工具、向攻击者控制的端点发送凭据或以其他方式改变行为，则属于范围之外。

如有可能，请包含精确的受影响路径、包版本或提交 SHA、配置，以及针对最新发布或最新 `main` 的概念验证。对于依赖报告，请包含证据表明随附的依赖受影响且该问题可通过 Pi 触达。对于暴露凭据的报告，请包含证据表明该凭据归 Earendil 所有或可授予对 Earendil 运营的基础设施或服务的访问权限。
