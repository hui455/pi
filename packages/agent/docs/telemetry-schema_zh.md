# Pi Agent 遥测模式

<!-- Generated by generate-telemetry-docs.ts. Do not edit manually. -->

## AI 请求模式

模式版本：1

### `pi.ai.request`

对 AI 提供商的一次逻辑请求

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 报错条件：操作抛出异常或返回错误结果

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.ai.operation` | `string` | 是 | stream, fetch_deferred, cancel_deferred, generate_images |  | 逻辑提供商操作 |
| `pi.ai.provider` | `string` | 是 |  |  | 选定的提供商 id |
| `pi.ai.model` | `string` | 是 |  |  | 请求的模型 id |
| `pi.ai.api` | `string` | 是 |  |  | 提供商 API id |
| `pi.ai.streaming` | `boolean` | 是 |  |  | 该操作是否返回流 |
| `pi.ai.deferred` | `boolean` | 否 |  |  | 该操作是否请求或参与延迟执行 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.ai.response.model` | `string` |  |  | 实际响应的模型 |
| `pi.ai.response.id` | `string` |  | 高基数 | 提供商响应 id |
| `pi.ai.response.stop_reason` | `string` | stop, length, tool_use, error, aborted, deferred |  | 规范化后的最终响应原因 |
| `pi.ai.http.status_code` | `number` |  |  | 最终 HTTP 状态码 |
| `pi.ai.usage.input_tokens` | `number` |  |  | 报告的输入 token 数 |
| `pi.ai.usage.output_tokens` | `number` |  |  | 报告的输出 token 数 |
| `pi.ai.usage.cache_read_tokens` | `number` |  |  | 报告的缓存读取 token 数 |
| `pi.ai.usage.cache_write_tokens` | `number` |  |  | 报告的缓存写入 token 数 |
| `pi.ai.usage.reasoning_tokens` | `number` |  |  | 报告的推理 token 数 |
| `pi.ai.usage.total_tokens` | `number` |  |  | 报告的总 token 数 |
| `pi.ai.usage.cost` | `number` |  |  | 报告的总成本 |
| `pi.ai.stream.chunk_count` | `number` |  |  | 流式更新块数量 |
| `pi.ai.stream.time_to_first_chunk_ms` | `number` |  |  | 到首个更新块所经过的毫秒数 |
| `pi.ai.error.type` | `string` |  | 低基数 | 提供商或传输层错误类别 |

#### 事件

无已声明的 span 事件。

## Harness 模式

模式版本：1

### `pi.harness.run`

一次被接受的进程内运行调用

- 父级：root 或调用方自有的外部 span
- 默认状态：`ok`
- 报错条件：运行失败或抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.session.id` | `string` | 是 |  | 高基数 | 会话 id |
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.operation.recovery` | `boolean` | 是 |  |  | 该调用是否恢复持久化工作 |
| `pi.operation.kind` | `string` | 是 | run |  | 运行操作类型 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.operation.outcome` | `string` | completed, aborted, failed, suspended |  | 运行调用结果 |
| `pi.error.code` | `string` |  | 低基数 | 稳定的操作错误码 |
| `pi.error.type` | `string` |  | 低基数 | 低基数操作错误类别 |

#### 事件

无已声明的 span 事件。

### `pi.harness.compaction`

一次被接受的进程内手动压缩调用

- 父级：root 或调用方自有的外部 span
- 默认状态：`ok`
- 报错条件：压缩失败或抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.session.id` | `string` | 是 |  | 高基数 | 会话 id |
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.operation.recovery` | `boolean` | 是 |  |  | 该调用是否恢复持久化工作 |
| `pi.operation.kind` | `string` | 是 | compaction |  | 压缩操作类型 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.operation.outcome` | `string` | completed, declined, aborted, failed |  | 压缩调用结果 |
| `pi.error.code` | `string` |  | 低基数 | 稳定的操作错误码 |
| `pi.error.type` | `string` |  | 低基数 | 低基数操作错误类别 |

#### 事件

无已声明的 span 事件。

### `pi.harness.navigation`

一次被接受的进程内导航调用

- 父级：root 或调用方自有的外部 span
- 默认状态：`ok`
- 报错条件：导航失败或抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.session.id` | `string` | 是 |  | 高基数 | 会话 id |
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.operation.recovery` | `boolean` | 是 |  |  | 该调用是否恢复持久化工作 |
| `pi.operation.kind` | `string` | 是 | navigation |  | 导航操作类型 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.operation.outcome` | `string` | completed, declined, aborted, failed |  | 导航调用结果 |
| `pi.error.code` | `string` |  | 低基数 | 稳定的操作错误码 |
| `pi.error.type` | `string` |  | 低基数 | 低基数操作错误类别 |

#### 事件

无已声明的 span 事件。

### `pi.harness.checkpoint`

一次运行检查点

- 父级：`pi.harness.run`
- 默认状态：`ok`
- 报错条件：检查点工作抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.checkpoint.kind` | `string` | 是 | normal, failure_drain, abort_reconcile |  | 检查点用途 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| _none_ | | | | |

#### 事件

无已声明的 span 事件。

### `pi.harness.turn`

一次助手响应及其工具批次

- 父级：`pi.harness.run`
- 默认状态：`ok`
- 报错条件：轮次工作抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.turn.id` | `string` | 是 |  | 高基数 | 调用本地的轮次 id |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| _none_ | | | | |

#### 事件

无已声明的 span 事件。

### `pi.harness.step`

一次持久化重试尝试

- 父级：`pi.harness.turn`、`pi.harness.checkpoint`、`pi.harness.compaction`、`pi.harness.navigation`
- 默认状态：`ok`
- 报错条件：该尝试重试、失败或抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.step.kind` | `string` | 是 | assistant, compaction, branch_summary |  | 可重试的步骤类型 |
| `pi.step.attempt` | `number` | 是 |  |  | 从 1 开始的持久化尝试序号 |
| `pi.compaction.reason` | `string` | 否 | manual, threshold, overflow |  | 压缩触发原因 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.step.outcome` | `string` | succeeded, retry, failed, aborted, deferred, overflow |  | 尝试结果 |

#### 事件

无已声明的 span 事件。

### `pi.harness.tool`

一次原始阶段 2 工具执行

- 父级：`pi.harness.turn`、`pi.harness.run`
- 默认状态：`ok`
- 报错条件：原始阶段 2 执行返回错误

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.turn.id` | `string` | 否 |  | 高基数 | 调用本地的实时轮次 id |
| `pi.tool.name` | `string` | 是 |  |  | 工具名称 |
| `pi.tool.call_id` | `string` | 是 |  | 高基数 | 工具调用 id |
| `pi.tool.replay` | `string` | 是 | never, safe |  | 声明的重放策略 |
| `pi.tool.recovery` | `boolean` | 是 |  |  | 是否为恢复执行 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.tool.is_error` | `boolean` |  |  | 原始阶段 2 执行是否返回错误 |

#### 事件

无已声明的 span 事件。

### `pi.harness.hook`

一次已注册钩子处理器的调用

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 报错条件：处理器抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 否 |  | 高基数 | 接受时的持久操作 id |
| `pi.hook.name` | `string` | 是 | before_run, before_resume, before_run_end, transform_context, before_request, before_payload, after_response, before_tool, after_tool, before_compaction, before_navigation |  | 钩子名称 |
| `pi.hook.registration_id` | `string` | 否 |  |  | 稳定的钩子注册 id |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.hook.outcome` | `string` | completed, skipped, blocked, failed |  | 处理器结果 |

#### 事件

无已声明的 span 事件。

### `pi.harness.sleep`

一次重试延迟

- 父级：`pi.harness.step`、`pi.harness.run`
- 默认状态：`ok`
- 报错条件：休眠工作抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.operation.id` | `string` | 是 |  | 高基数 | 持久操作 id |
| `pi.sleep.delay_ms` | `number` | 是 |  |  | 请求的延迟毫秒数 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.sleep.outcome` | `string` | elapsed, aborted |  | 延迟结果 |

#### 事件

无已声明的 span 事件。

### `pi.harness.event_handler`

一次被动事件监听器调用

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 报错条件：监听器抛出异常

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.event.type` | `string` | 是 | run_start, run_resume, run_suspend, run_abort, run_end, fault, handler_error, turn_start, turn_end, retry_scheduled, retry_start, retry_end, message_start, message_update, message_end, tool_start, tool_update, tool_end, entry_added, write_pending, queue_update, fact_update, config_update, compaction_start, compaction_end, navigation_start, navigation_end, lane_created, usage | 低基数 | 已投递的 harness 事件类型 |
| `pi.lane.name` | `string` | 否 |  | 高基数 | 作用于 Lane 范围的事件所对应的 Lane 名称 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| _none_ | | | | |

#### 事件

无已声明的 span 事件。

### `pi.session.write`

一次已提交的会话变更

- 父级：root 或任意调用方 span
- 默认状态：`ok`
- 报错条件：存储拒绝该变更

#### 起始属性

| 名称 | 类型 | 必填 | 取值 | 备注 | 描述 |
|---|---:|---|---|---|---|
| `pi.lane.name` | `string` | 是 |  | 高基数 | Lane 名称 |
| `pi.operation.id` | `string` | 否 |  | 高基数 | 接受时的持久操作 id |
| `pi.session.mutation` | `string` | 是 | entry, record, lane, fact |  | 会话变更类型 |
| `pi.session.item_type` | `string` | 否 |  |  | 条目、记录、Lane 或事实的子类型 |

#### 结束属性

所有结束属性都是可选的完成性补充。

| 名称 | 类型 | 取值 | 备注 | 描述 |
|---|---|---|---|---|
| `pi.session.seq` | `number` |  |  | 暴露时的已提交会话序号 |

#### 事件

无已声明的 span 事件。
