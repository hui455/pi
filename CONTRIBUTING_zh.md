# 为 pi 做贡献

本指南的存在是为了给双方节省时间。

## 理念

首先最重要的一点：**pi 的核心是极简的**。

如果你的功能不属于核心，它应该做成一个扩展。臃肿核心的 PR 很可能会被拒绝。

Pi 的核心存在是为了保持极简且可扩展，以便能够被扩展影响和操纵。即使是扩展的钩子点（hook point）也应该经过深思熟虑和讨论，以避免引入无法维护的臃肿和复杂的交互。

## 唯一规则

**你必须理解你的代码。** 如果你无法解释你的改动做了什么、以及它们如何与系统其余部分交互，你的 PR 将被关闭。

使用 AI 写代码没问题。提交自己都不理解的 AI 生成的垃圾代码是不行的。

如果你使用代理，请从 `pi` 根目录运行它，这样它会自动读取 `AGENTS.md`。你的代理必须遵守该文件中的规则和指南。

## 贡献门槛

新贡献者的所有 issue 和 PR 默认都会被自动关闭。

周五至周日提交的 issue 不保证会被审查。如果事情紧急，请在 Discord 上询问：https://discord.com/invite/3cU7Bz4UPx

维护者每天都会审查被自动关闭的 issue 并重新打开值得处理的。不符合下面质量标准的 issue 不会被重新打开，也不会收到回复。

批准通过维护者在 issue 上的回复进行：

- `lgtmi`：你未来的 issue 将不会被自动关闭
- `lgtm`：你未来的 issue 和 PR 将不会被自动关闭

该命令必须位于回复的开头（可选地在一次或多次 `@username` 提及之后）或结尾。`lgtmi` 不授予提交 PR 的权利。只有 `lgtm` 授予提交 PR 的权利。

## Issue 的质量标准

如果你开启一个 issue，必须使用两个 GitHub issue 模板之一。

如果你开启一个 issue，请保持简短、具体且值得一读。

- 保持简洁。如果一屏放不下，就太长了。
- 用你自己的话写（不要用 LLM 生成文字，如果必须使用，请用明确标注为 AI 的评论跟进）。
- 清楚地陈述 bug 或请求。
- 解释它为什么重要。
- 如果你想自己实现这个改动，请说明。

如果 issue 是真实的并且写得很好，维护者可能会重新打开它，或在上述命令位置回复 `lgtmi` 或 `lgtm`。

## 封禁

如果你两次无视本文档，或者用代理生成的 issue 刷爆 tracker，你的 GitHub 账号将被永久封禁。

如果你通过自动化发送大量 issue，你的 GitHub 账号将被永久封禁。没有回头路。

## 提交 PR 之前

除非你已经通过维护者在上述命令位置使用 `lgtm` 批准，否则不要开启 PR。

提交 PR 之前：

```bash
npm run check
./test.sh
```

两者都必须通过。

不要编辑 `CHANGELOG.md`。changelog 条目由维护者添加。

如果你要向 `packages/ai` 添加新提供商，请参阅 `AGENTS.md` 了解必需的测试。

## 有疑问？

在 [Discord](https://discord.com/invite/nKXTsAcmbT) 上提问。

## FAQ

### 为什么新的 issue 和 PR 会被自动关闭？

pi 收到的 issue 超过了维护者能实时负责任地审查的数量。许多报告不符合本指南中的质量标准，或不遵循 CONTRIBUTING.md。有些是被代理盲目地扔到仓库里的，而不是由提交者审查和打磨。自动关闭创建了一个缓冲，让维护者可以按自己的节奏审查 tracker，并重新打开符合质量标准的 issue。

### 为什么周末的 issue 优先级更低？

我们在工作时间内对 tracker 进行分诊。这意味着周末会积累更多的 issue。周五至周日提交的任何内容都可能在周一的审查队列中被遗漏或给予较低优先级。如果问题紧急，请在 Discord 上询问，并附上简短版本、复现步骤和相关日志。

### 为什么有些 issue 得不到回复？

回复本身也是维护工作。低信号 issue、不清晰的报告、重复项以及不遵循本指南的 issue 可能会在不经讨论的情况下被关闭。这为可复现的 bug、深思熟虑的请求，以及已经付出努力使报告可执行的贡献者保留了时间。

### 为什么不让 AI 分诊一切？

AI 可以帮助归组重复项、总结报告和发现缺失的信息。但它不能被信任去做最终的维护者决策。精心制作的 AI 生成的 issue 仍然可能是错误的、误导性的或调查成本高昂的。人工审查仍然是最终关卡。

### 这是否对贡献者怀有敌意？

不是。它是对倦怠和 tracker 垃圾信息的防护栏。简短、具体、可复现的 issue 是受欢迎的。深思熟虑的贡献是受欢迎的。自动化的垃圾、傲慢自大和大量低质量的报告是不受欢迎的。

## 在哪里可以了解计划？

Earendil 使用 RFC 来讨论较大的变更。并非所有 RFC 都是公开的，但相当一部分是。可以在 [rfc.earendil.com](https://rfc.earendil.com/keyword/pi/) 找到它们。
